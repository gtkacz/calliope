# Directory name of the per-workspace shadow git store used for file version
# history. Kept separate from the user's own ".git" so Calliope never reads,
# writes, or pollutes their real repository.
VERSION_STORE_DIRNAME = ".calliope-git"
